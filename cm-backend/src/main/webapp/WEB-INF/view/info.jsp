{
    "application" : {
        "name"      : "${appName}",
        "version"   : ""
    },
    "security": {
    "csrf-param": "${_csrf.parameterName}",
    "csrf-token": "${_csrf.token}"
    }
}    
    
